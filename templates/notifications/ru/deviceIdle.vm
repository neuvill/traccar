#set($subject = "$esc.html($device.name): на холостом ходу")
#set($durationMinutes = $event.attributes.duration / 60000)
#set($digest = "$device.name на холостом ходу $durationMinutes мин с $dateTool.format('dd.MM.yyyy HH:mm:ss', $event.eventTime, $locale, $timezone)")
<!DOCTYPE html>
<html>
<body>
Устройство: $esc.html($device.name)<br>
На холостом ходу<br>
Время на холостом ходу: $durationMinutes мин<br>
Время: $dateTool.format('dd.MM.yyyy HH:mm:ss', $event.eventTime, $locale, $timezone)<br>
Координаты: <a href="$webUrl/event/$event.id">#{if}($position.address)$position.address#{else}$position.latitude&deg;, $position.longitude&deg;#{end}</a><br>
<br>
<a href="$webUrl/settings/notifications?token=$token">Отписаться</a>
</body>
</html>
