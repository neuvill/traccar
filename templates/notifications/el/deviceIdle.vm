#set($subject = "Η Συσκευή $esc.html($device.name) σε Ρελαντί")
#set($durationMinutes = $event.attributes.duration / 60000)
#set($digest = "Η Συσκευή $device.name σε ρελαντί για $durationMinutes λεπτά στις $dateTool.format('dd-MM-yyyy HH:mm:ss', $event.eventTime, $locale, $timezone)")
<!DOCTYPE html>
<html>
<body>
Συσκευή: $esc.html($device.name)<br>
Σε ρελαντί<br>
Διάρκεια ρελαντί: $durationMinutes λεπτά<br>
Ώρα: $dateTool.format('dd-MM-yyyy HH:mm:ss', $event.eventTime, $locale, $timezone)<br>
Σημείο: <a href="$webUrl/event/$event.id">#{if}($position.address)$position.address#{else}$position.latitude&deg;, $position.longitude&deg;#{end}</a><br>
<br>
<a href="$webUrl/settings/notifications?token=$token">Απεγγραφή</a>
</body>
</html>
